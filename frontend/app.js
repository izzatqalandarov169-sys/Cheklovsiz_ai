const tabs=document.querySelectorAll('.tab');const panels=document.querySelectorAll('.panel');tabs.forEach(t=>t.onclick=()=>{tabs.forEach(x=>x.classList.remove('active'));panels.forEach(x=>x.classList.remove('active'));t.classList.add('active');document.getElementById(t.dataset.mode).classList.add('active')});
const form=document.getElementById('chatForm');const input=document.getElementById('chatInput');const messages=document.getElementById('messages');form.onsubmit=e=>{e.preventDefault();const text=input.value.trim();if(!text)return;messages.insertAdjacentHTML('beforeend',`<div class="msg user"></div>`);messages.lastElementChild.textContent=text;input.value='';setTimeout(()=>messages.insertAdjacentHTML('beforeend','<div class="msg ai">Backend AI hali ulanmagan. Keyingi bosqichda local model API qo‘shiladi.</div>'),300)};
document.getElementById('generate').onclick=()=>{const p=document.getElementById('videoPrompt').value.trim();const s=document.getElementById('videoStatus');s.textContent=p?`Demo: “${p}” — ${document.getElementById('duration').value} soniyalik video uchun so‘rov tayyor. Backend video model ulanadi.`:'Avval video prompt yozing.'};
